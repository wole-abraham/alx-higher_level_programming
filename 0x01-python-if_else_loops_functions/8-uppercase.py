#!/usr/bin/python3
def uppercase(str):
    for ch in str:
        if ord(ch) == 32:
            print("{}".format(ch), end='')
        elif ord(ch) > 90:
            print("{}".format(chr(a)), end='')
